[project]
name = "bot-gateway"
version = "0.1.0"
description = "Telegram bot gateway"
requires-python = ">=3.10"

dependencies = [
    "aiogram>=3.15,<4.0",
    "httpx>=0.28,<1.0",
]

[tool.uv]
package = false