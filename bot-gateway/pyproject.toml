[project]
name = "bot-gateway"
version = "0.1.0"
description = "Telegram bot gateway"
requires-python = ">=3.10"

dependencies = [
    "aiogram",
    "httpx",
    "python-dotenv",
    "watchfiles>=1.2.0",
]

[tool.uv]
package = false
